# Project Structure

This repository now includes a starter folder layout for:

- `src/ml_package/` for package source code
- `notebooks/` for example notebooks by topic
- `tests/` for unit tests by topic
- `.github/workflows/` for CI automation
- `examples/` for script-style algorithm demos
- `docs/` for supporting documentation
